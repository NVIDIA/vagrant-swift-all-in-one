[DEFAULT]
disable_fallocate = true
workers = 1

[pipeline:main]
pipeline = <%= @zipkin %> recon account-server

[app:account-server]
use = egg:swift#account
replication_server = <%= @replication_server %>

[filter:recon]
use = egg:swift#recon
