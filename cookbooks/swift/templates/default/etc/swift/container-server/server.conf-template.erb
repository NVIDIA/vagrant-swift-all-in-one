[DEFAULT]
disable_fallocate = true
workers = 1

[pipeline:main]
pipeline = <% if @tracing -%>trace <% end -%> recon container-server

[app:container-server]
use = egg:swift#container
<% if ! @replication_server -%>
replication_server = false
<% end -%>

[filter:recon]
use = egg:swift#recon

<% if @tracing -%>
[filter:trace]
use = egg:swift#trace
trace_exporter_module = egg:swift#otel_exporter
trace_exporter_config = /etc/swift/otel_trace_exporter.json
trace_name = Swift container-server
<% end -%>
