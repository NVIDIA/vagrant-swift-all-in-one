[DEFAULT]
mount_check = false
disable_fallocate = true
workers = 1

[pipeline:main]
pipeline = recon container-server

[app:container-server]
use = egg:swift#container

[filter:recon]
use = egg:swift#recon

[container-replicator]
# vm_test_mode = yes

[container-updater]

[container-auditor]

[container-sync]
internal_client_conf_path = /etc/swift/internal-client.conf

[container-sharder]
shard_container_size = 50
